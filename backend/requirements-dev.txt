# Test + lint tooling for the backend. Pinned, ARM64-safe, CI-only (never shipped
# in the api/scheduler image). Runtime deps (pandas/numpy/pyyaml/pydantic/sqlalchemy)
# come from requirements*.txt; this file only adds the harness.
pytest==8.3.4
pytest-asyncio==0.24.0
ruff==0.8.4
